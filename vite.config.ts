import { defineConfig } from 'vite'

// Voeg hier de configuratie voor GitHub Pages toe
export default defineConfig({
  base: '/SneupHal/', // Zorg dat dit exact je repositorynaam is, in jouw geval 'SneupHal'
})
